{
	//set the bound port
	"port":5200,
	//default file when ask /
	"default file":"index.html",
	//if fork model,use "MULTIPLEXING" or "FORK" or"THREAD"
	"model":"MULTIPLEXING",
	//if long connect
	"long connect":true,
	//if run in background
	"background":false,
	//if use guard
	"guard":false,
	//remove path array
	"delete path":[],
	//replace the path,the arr must is a objext and have old and replace 
	"replace":[],
	//set agent array
	"reverse proxy":[{"host":"127.0.0.1","port":5201,"path":""}]
}
